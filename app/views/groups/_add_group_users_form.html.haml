= form_tag add, method: :post do
  - User.all.each do |user|
    .user-checkbox
      = check_box_tag 'user_ids[]', user.id
      = label_tag "user_ids_#{user.id}", user.email

  = submit_tag "Add Selected Users"
